import axios from 'axios';
import token from './userService';
const baseUrl = 'http://localhost:3001/api/orders';

const getUserOrders = () => {
  if (token) {
      const res = 
  }
  return null;
};
